public class Solution {
    public int linearSearch(int[] A, int B) {
        int c=0,n=A.length;
        for(int i=0;i<n;i++){
            if(A[i]==B){
                c=1;
            }
        }
        return c;  
    }
}
